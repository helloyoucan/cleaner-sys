export enum EnumBranchStatus {
  close = 0, //关闭中
  open = 1, //营业中
  rest = 2, //休息中
}

export enum EnumExtraServiceStatus {
  disable = 0, //可用
  enable = 1, //不可用
}

export enum EnumSex {
  female = 0,
  male = 1,
}
export enum EnumWarriorStatus {
  disable = 0, //禁用（禁止接单）
  enable = 1,
}
